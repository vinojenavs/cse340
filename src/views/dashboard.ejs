<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <p>Name: <%= name %></p>
    <p>Username: <%= email %></p>
    <% if (user && user.role_name === 'admin') { %>
        <a href="/users-page" class="btn">View Registered Users</a>
    <% } %>
</main>
<%- include('partials/footer') %>